# ── 本机工具状态（不含产品代码，且含本机绝对路径，不随仓库分发）──
.workbuddy/
.wbapp_*.genie

# ── 依赖与构建产物 ──
node_modules/
dist/
build/
*.tgz

# ── 临时与调试文件 ──
_test/_tmp_*
_test/_commitmsg.txt
*.tmp
*.log

# ── 编辑器与操作系统 ──
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini

# ── 本地密钥/环境变量（切勿提交）──
.env
.env.local
